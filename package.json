{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.2",
    "react-color": "^2.14.1",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4",
    "webfontloader": "^1.6.28"
  },
  "scripts": {
    "build-css": "node-sass src/ -o src/",
    "css": "npm run build-css && node-sass src/ -o src/ --watch",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "node-sass": "^4.9.0",
    "styled-components": "^3.2.6"
  }
}
